The custom synthesis trees, one for each collection. Each directory contains a .tre file (newick format, labels are 'ottname ottid'). The details of the synthesis run, including support and conflict for the edges, are in the annotations.json file. 

